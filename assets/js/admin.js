(function(){
'use strict';
const qs=(s,c=document)=>c.querySelector(s), qsa=(s,c=document)=>Array.from(c.querySelectorAll(s));

const root=qs('#wcar-report-root');
if(root){const rid=root.dataset.reportId||'report',key='wcar_columns_'+rid,toggles=qsa('.wcar-column-toggle',root),table=qs('.wcar-report-table',root);if(toggles.length&&table){let hidden=[];try{hidden=JSON.parse(localStorage.getItem(key)||'[]');}catch(e){} const apply=()=>{toggles.forEach((t,i)=>{const hide=!t.checked;qsa('tr',table).forEach(tr=>{const cell=tr.children[i];if(cell)cell.style.display=hide?'none':'';});});};toggles.forEach((t,i)=>{t.checked=!hidden.includes(i);t.addEventListener('change',()=>{const h=toggles.map((x,j)=>x.checked?null:j).filter(x=>x!==null);localStorage.setItem(key,JSON.stringify(h));apply();});});apply();}}

const printBtn=qs('#wcar-print'); if(printBtn) printBtn.addEventListener('click',()=>window.print());
qsa('.wcar-confirm-delete').forEach(a=>a.addEventListener('click',e=>{if(!window.confirm((window.WCARAdmin&&WCARAdmin.confirmDelete)||'Delete this item?'))e.preventDefault();}));
qsa('.wcar-table-search').forEach(input=>input.addEventListener('input',()=>{const table=input.closest('.wcar-card').querySelector('table');const term=input.value.toLowerCase();qsa('tbody tr',table).forEach(tr=>tr.style.display=tr.textContent.toLowerCase().includes(term)?'':'none');}));
qsa('[data-wcar-list-target]').forEach(input=>{const form=input.form; if(!form)return; form.addEventListener('submit',()=>{const name=input.dataset.wcarListTarget+'[]';qsa('input[data-wcar-generated="1"]',form).forEach(x=>x.remove());input.value.split(',').map(x=>x.trim()).filter(Boolean).forEach(v=>{const h=document.createElement('input');h.type='hidden';h.name=name;h.value=v;h.dataset.wcarGenerated='1';form.appendChild(h);});});});
const dataEl=qs('#wcar-chart-data'); if(dataEl){let data={};try{data=JSON.parse(dataEl.textContent||'{}');}catch(e){} qsa('.wcar-chart').forEach(canvas=>draw(canvas,data[canvas.dataset.chart]||[],canvas.dataset.chart));}
function draw(canvas,rows,type){const dpr=window.devicePixelRatio||1,w=canvas.clientWidth||600,h=260;canvas.width=w*dpr;canvas.height=h*dpr;const c=canvas.getContext('2d');c.scale(dpr,dpr);c.clearRect(0,0,w,h);if(!rows.length){c.fillText('No data',20,30);return;}let labels=[],values=[];rows.forEach(r=>{if(type==='trend'){labels.push(r.period||'');values.push(Number(r.net_sales||0));}else if(type==='status'){labels.push(r.status||'');values.push(Number(r.orders||0));}else if(type==='products'){labels.push((r.product||'').slice(0,18));values.push(Number(r.net_sales||0));}else{labels.push((r.customer_segment||'')+' '+(r.registration||''));values.push(Number(r.customers||0));}});const max=Math.max(...values,1),pad=36,plotW=w-pad*2,plotH=h-pad*2;c.strokeStyle='#8c8f94';c.lineWidth=1;c.beginPath();c.moveTo(pad,pad);c.lineTo(pad,h-pad);c.lineTo(w-pad,h-pad);c.stroke();if(type==='trend'&&values.length>1){c.strokeStyle='#2271b1';c.lineWidth=2;c.beginPath();values.forEach((v,i)=>{const x=pad+(i/(values.length-1))*plotW,y=h-pad-(v/max)*plotH;i?c.lineTo(x,y):c.moveTo(x,y);});c.stroke();}else{const bw=Math.max(4,plotW/values.length*.65);values.forEach((v,i)=>{const x=pad+(i+.5)*plotW/values.length-bw/2,bh=(v/max)*plotH;c.fillStyle='#2271b1';c.fillRect(x,h-pad-bh,bw,bh);});}c.fillStyle='#50575e';c.font='10px sans-serif';const step=Math.max(1,Math.ceil(labels.length/8));labels.forEach((lab,i)=>{if(i%step)return;const x=pad+(i+(type==='trend'?0:.5))*plotW/Math.max(type==='trend'?labels.length-1:labels.length,1);c.save();c.translate(x,h-pad+10);c.rotate(-.35);c.fillText(lab,0,0);c.restore();});}
})();
